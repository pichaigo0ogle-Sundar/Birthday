name: Sync to Hugging Face Hub
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Push to Hub
        uses: huggingface/hub-sync@v0.1.0
        with:
          github_repo_id: pichaigo0ogle-Sundar/Birthday
          huggingface_repo_id: Naren1100/birthday
          hf_token: ${{ secrets.HF_TOKEN }}
          space_sdk: docker
